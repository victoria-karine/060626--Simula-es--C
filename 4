
#include <stdio.h> 
int main () {
  int i, status; 
  int concluidas = 0, pendentes = 0;
  
  //Implemente aqui um FOR para repetir 8x
  // Leia status.
  //Se status == 1, incremento concluidas.
  //Caso contrario, incremente pendentes.
  
  printf ("Concluidas: %d", concluidas );
  prontf ("Pendentes: %d", pendentes);
  
  
     return 0;
     
}
