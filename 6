
#include <stdio.h> 

int main () {
int prioridade, continuar;
int baixa = 0, int media= 0, int alta = 0;

// Implemente aqui DO WHILE
// Leia prioridade.
// Se prioridade == 1, conte baixa.
// Se prioridade == 2, conte média.
// Se prioridade == 3, conte alta.
// Senao exiba PRIORIDADE INVALIDA
printf (" Prioridade Baixa: %d", baixa);
printf("Prioridade Média: %d", media);
printf ("Prioridade Alta: %d", alta); 

    return 0; 
}
