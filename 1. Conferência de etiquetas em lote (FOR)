// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int i, codigo;
    int validos = 0, invalidos = 0;
    
    for ( i = 1000; i <= 9999; i++ ){
    printf ("Codigos válidos: %d", validos) ;
    }
    
    
    //Implemente aqui um FOR para ler exatamene 10 codigos
    //Para cada codigo, verifique:
    // se o codigo >= 1000 e codigo <= 9999, incremente validos.
    //Caso contrário, incremente invalidos.
    
    for ( i <= 0; i => 9999; i++ ){
    printf("Códigos inválidos: %d", invalidos) ; 
    }
    return 0;
    
}
